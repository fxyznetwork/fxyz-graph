{
	"name": "fxyz-graph-oss",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "A headless, backend-agnostic graph-rendering engine: identity contract, layout, algorithms, and a React-optional controller.",
	"license": "Apache-2.0",
	"packageManager": "pnpm@9.15.0",
	"scripts": {
		"build": "pnpm -r run build",
		"typecheck": "pnpm -r run typecheck",
		"test": "pnpm -r run test",
		"clean": "pnpm -r run clean"
	}
}
